# AI ENGINEERING — PARTIE 05
## Git + GitHub — ingénierie logicielle

Créer le cursus Git + GitHub en refusant l'approche « add/commit/push » mémorisée sans compréhension.

Expliquer le modèle interne : repository ; working tree ; staging area/index ; commit comme snapshot ; historique/DAG ; hash ; HEAD ; branches comme références ; merge fast-forward et 3-way ; rebase ; conflits ; reflog ; remote ; origin ; fetch/pull/push ; tracking branches.

Puis GitHub : repositories ; issues ; pull requests ; reviews ; tags/releases ; secrets ; permissions ; GitHub Actions ; CI/CD.

Projet : versionner le projet recettes depuis zéro, créer une branche, modifier, merger, provoquer un conflit, le résoudre, créer une PR et faire passer une CI.

Exiger des preuves pratiques et expliquer ce que Git stocke réellement.

### RÈGLE PÉDAGOGIQUE
Modèle mental → définition technique → fonctionnement → exemple → code → erreur volontaire → debug → test → sécurité → performance → architecture → exercice → preuve.

### PROJET FIL ROUGE
Restaurant Intelligence Platform.
