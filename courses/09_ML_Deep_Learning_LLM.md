# AI ENGINEERING — PARTIE 09
## Machine Learning, Deep Learning et LLM

### MACHINE LEARNING
Features ; labels ; train/validation/test ; régression ; arbres ; overfitting ; underfitting ; regularization ; loss ; métriques ; optimisation.

### DEEP LEARNING
Neurone ; couche ; réseau ; activations ; gradient descent ; backpropagation ; CNN/RNN seulement si utile ; Transformers ; attention.

### LLM
Tokens ; tokenization ; embeddings ; attention ; context window ; inference ; sampling ; temperature ; top-p ; structured outputs ; function/tool calling.

Construire le bon modèle mental avant l'utilisation des APIs. Faire la distinction entre « utiliser un LLM » et « comprendre un système LLM ».

Projet : wrapper robuste autour d'un LLM avec structured output, validation, retry, logs et tests.

Évaluer le système, pas seulement la présence d'une réponse.

### RÈGLE PÉDAGOGIQUE
Modèle mental → définition technique → fonctionnement → exemple → code → erreur volontaire → debug → test → sécurité → performance → architecture → exercice → preuve.

### PROJET FIL ROUGE
Restaurant Intelligence Platform.
